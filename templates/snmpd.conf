#  Listen for connections from anywhere.
agentAddress  udp:161,udp6:[::1]:161

# ACL's
{% for acl in acls -%}
{{ acl }}
{% endfor %}

# System information, everything but location and contact is automatically generated by SNMPD

sysLocation    {{ sysLocation }}
sysContact     {{ sysContact }}
sysServices    72

# Other configuration

{% for extra in other -%}
{{ extra }}
{% endfor %}
